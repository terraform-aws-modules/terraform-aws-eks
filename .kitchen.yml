---
driver:
  name: "terraform"
  command_timeout: 3000
  root_module_directory: "examples/basic"

provisioner:
  name: "terraform"

platforms:
  - name: "aws"

verifier:
  name: "awspec"

suites:
  - name: "default"
    verifier:
      name: "awspec"
      patterns:
      - "test/integration/default/test_eks.rb"
